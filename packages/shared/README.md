# Green Lab Shared (placeholder)

Reserved for shared types and channel adapters that get used by both the
backend (`apps/api`) and the eval harness (`packages/evals`). Phase 1 keeps
the type definitions colocated with their primary use; this package will
mature when email/whatsapp adapters in Phase 2/3 need cross-cutting types.
